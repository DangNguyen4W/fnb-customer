pre-commit:
  parallel: true
  staged_fixed: true
  jobs:
    - name: lint checking
      run: npm run lint:fix -- {staged_files}
      glob: "*.{js,ts,jsx,tsx}"

commit-msg:
  jobs:
    - name: commit message checking
      run: npx commitlint --edit {1}

# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
