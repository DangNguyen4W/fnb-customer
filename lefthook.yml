pre-commit:
  parallel: true
  jobs:
    - run: npm run lint -- {staged_files}
      glob: "*.{js,ts,jsx,tsx}"

# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
